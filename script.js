const intro=document.querySelector('#intro');const skip=document.querySelector('#skip');const sound=document.querySelector('#sound');const loading=document.querySelector('#loadingText');const messages=['TUNING THE VTEC...','ENTERING MIAMI...','LOADING NEW ARRIVALS...','BUILD COMPLETE'];let message=0;const cycle=setInterval(()=>{message=Math.min(message+1,messages.length-1);loading.textContent=messages[message]},1450);function finish(){intro.classList.add('done');clearInterval(cycle)}skip.addEventListener('click',finish);setTimeout(finish,6800);sound.addEventListener('click',()=>{sound.textContent=sound.textContent==='SOUND OFF'?'SOUND ON':'SOUND OFF'});document.querySelectorAll('.shirt').forEach(card=>{card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect();card.style.setProperty('--rx',`${(e.clientY-r.top)/r.height*-5+2.5}deg`);card.style.setProperty('--ry',`${(e.clientX-r.left)/r.width*5-2.5}deg`)});card.addEventListener('pointerleave',()=>{card.style.setProperty('--rx','0deg');card.style.setProperty('--ry','0deg')})});
